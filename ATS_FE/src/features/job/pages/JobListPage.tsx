/**
 * Job list page placeholder.
 * Will display job postings with filtering, search, and CRUD actions.
 */
const JobListPage = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Job Postings</h1>
      <p className="mt-2 text-muted-foreground">
        Job listing with search, filter, and management actions will be displayed here.
      </p>
    </div>
  );
};

export default JobListPage;
